# yml file generated by Microsoft CoPilot
name: Update Submodules

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *" # Run daily at midnight (adjust the schedule as needed)
jobs:
  update-submodules:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Set up author
        run: |
          git config user.email "braulio00001@gmail.com"
          git config user.name "Braulee7"

      - name: Update submodules
        run: |
          git submodule update --remote
          git add CS*
          git commit -m "Update submodules"
          git push
