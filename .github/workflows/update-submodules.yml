# yml file generated by Microsoft CoPilot
name: Update Submodules

on:
  schedule:
    - cron: "0 0 * * *" # Run daily at midnight (adjust the schedule as needed)

jobs:
  update-submodules:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Update submodules
        run: |
          git submodule update --remote --merge
          git add child_repo1
          git commit -m "Update child_repo1 submodule"
          git push
